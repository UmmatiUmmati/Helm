# Documentation: https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ummati.fullname" . }}
  labels:
    {{- include "ummati.defaultLabels" . | nindent 4 }}
spec:
  type: {{ .Values.ummati.service.type }}
  ports:
    - port: {{ .Values.ummati.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    name: {{ include "ummati.fullname" . }}