apiVersion: v1
kind: Service
metadata:
  name: {{ include "ummati.fullname" . }}
  labels:
    app: {{ include "ummati.name" . }}
    chart: {{ include "ummati.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "ummati.name" . }}
    release: {{ .Release.Name }}
